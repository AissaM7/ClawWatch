{
  "id": "clawwatch",
  "name": "ClawWatch",
  "description": "Deep observability plugin — streams LLM calls, tool executions, and diagnostics to the ClawWatch dashboard",
  "version": "0.1.0",
  "emoji": "👁️",
  "shape": "hook-only",
  "extensions": ["./index.ts"],
  "homepage": "https://github.com/clawwatch/openclaw-plugin",
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable or disable the ClawWatch plugin"
      }
    }
  }
}
