"""Risk scoring placeholder — actual scoring lives in the UI (TypeScript).

This module exists as a placeholder in the plugin package for potential
future server-side risk checks. Currently all risk scoring is done
deterministically in the frontend.
"""
